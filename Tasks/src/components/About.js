import React from "react"; // Required for JSX

const About = () => (
  <div className="container">
    <h2>About Tasks</h2>
    <p>A demo app for react and related technologies</p>
  </div>
);

export default About;
